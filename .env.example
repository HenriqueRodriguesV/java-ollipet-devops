# Copie para ".env" e ajuste os valores. O ".env" nunca deve ser commitado
# (ja esta no .gitignore) porque carrega credenciais do banco.
DB_NAME=ollipet
DB_USER=ollipet
DB_PASSWORD=troque-esta-senha
APP_SECURITY_JWT_SECRET=troque-esta-chave-com-pelo-menos-256-bits
FIREBASE_PROJECT_ID=
